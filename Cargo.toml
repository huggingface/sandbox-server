[package]
name = "sbx-server"
version = "0.1.0"
edition = "2021"

[dependencies]
serde_json = "1"
libc = "0.2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
